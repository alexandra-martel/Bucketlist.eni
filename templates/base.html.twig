<!doctype html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">


    <title>{% block title %}Bucket-List{% endblock %}</title>

    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Hachi+Maru+Pop&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
    <link rel="icon" href="{{ asset('img/favicon.png') }}">

</head>
<body>
    <header>
        <a href="{{ path('main_home') }}" title="Go back home">
        <img src="{{ asset('img/logo.png') }}" alt="Bucketlist"> Bucketlist
        </a>
        <nav>
            <a href="{{ path('main_home') }}" title="Go back home">Home</a>
            <a href="{{ path('wish_list') }}" title="The wish list">Wishes</a>
            <a href="{{ path ('wish_new') }}" title="New wish">New wish</a>
            <a href="{{ path('main_about_us') }}" title="About us">About Us</a>
        </nav>
    </header>
    <main>
        {% block body %}{% endblock %}
    </main>
    <footer>
        &copy;Bucketlist {{ "now"|date("Y") }}
    </footer>
</body>
</html>
