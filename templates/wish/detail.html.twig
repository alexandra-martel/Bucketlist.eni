{% extends 'base.html.twig' %}

{% block title %}
   {{ wish.title }} | {{ parent() }}
{% endblock %}

{% block body %}
    <h1>{{ wish.title }}</h1>
    <div class="wish-description">{{ wish.description|nl2br }}</div>
    <div>Créer par {{ wish.author }} le
    {{ wish.dateCreated|date('d-m-Y') }}</div>
{% endblock %}

