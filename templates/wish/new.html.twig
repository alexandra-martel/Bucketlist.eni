{% extends 'base.html.twig' %}

{% block title %}
     {{ parent() }} | New wish
{% endblock %}

{% block body %}
    <h1>New wish</h1>

    <div>
        {{ form(wishForm, {attr: {'novalidate': 'novalidate'}}) }}
    </div>

{% endblock %}

